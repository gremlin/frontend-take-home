/**
 * Header.tsx
 *
 * This component represents the header of the application.
 * Currently, it's a simple header element with no additional content.
 */
import React from 'react';

import './Header.scss';

/**
 * Header component.
 *
 * This is a functional component that renders the application's header.
 * Additional content and functionality can be added to this component as needed.
 */
const Header: React.FC = () => {

    return (
        <header />
    )
}

export default Header;